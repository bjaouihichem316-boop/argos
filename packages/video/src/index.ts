/** @argos/video — Playwright + FFmpeg + TTS (Phase 5). */
export const VIDEO_PHASE = 5 as const;

/** Phase 5 stub — implemented in Phase 5. */
export async function renderVideo(_jobId: string): Promise<never> {
  throw new Error("@argos/video.renderVideo — Phase 5 not implemented yet (see docs/ROADMAP.md)");
}

/** @argos/payments — paylix SDK wrapper (Phase 6). */
export const PAYMENTS_PHASE = 6 as const;

/** Phase 6 stub — implemented in Phase 6. */
export async function createIntent(_input: unknown): Promise<never> {
  throw new Error("@argos/payments.createIntent — Phase 6 not implemented yet (see docs/ROADMAP.md)");
}

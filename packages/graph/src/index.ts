/** @argos/graph — Neo4j + GraphRAG (Phase 3). */
export const GRAPH_PHASE = 3 as const;

/** Phase 3 stub — implemented in Phase 3. */
export async function upsertEntity(_input: unknown): Promise<never> {
  throw new Error("@argos/graph.upsertEntity — Phase 3 not implemented yet (see docs/ROADMAP.md)");
}
